// Re-exported from the shared notify client (extracted 2026-09-05, the
// friends spec's prerequisite): the code moved, this game's import paths
// did not — and every game that adopts invites imports the same package.
export * from '@game-host/notify/client/api';
