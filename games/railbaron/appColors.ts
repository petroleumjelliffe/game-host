// The PWA colors, root-level like basePath.ts so vite.config.ts and the
// manifest generator share one copy. They mirror src/index.css's body:
// near-black board, warm off-white flap text — the split-flap departures
// board is the whole visual identity, and the installed icon carries it.
export const THEME_COLOR = '#0a0a0a';
export const BACKGROUND_COLOR = '#0a0a0a';
